## 更新内容

### 新功能

- 大卡片显示方式：每个监测项目可在设置中切换「列表行 / 大卡片」，大卡片常显大字主值、实时走势与 TOP 进程，呼出面板即一眼扫读，无需点击展开
- 默认展开：每个监测项目可单独设置呼出面板时默认处于展开状态
- 内存压力显示模式：内存模块主指标可在「压力等级 / 使用率」间切换，压力模式下右侧显示与活动监视器同口径的连续压力曲线
- 菜单栏新增「内存压力」指标（连续压力百分比，定宽对齐）

### 优化与体验

- 面板走势图新增坐标轴参照（X 轴实线 + Y 轴渐隐），填充渐变更清晰
- 大卡片 TOP 进程列表固定 5 行占位，数据加载过程高度不跳变

