# Tasks

## 1. 顺序模型与持久化

- [x] 1.1 定义顶层及“模块／分页／连续指标区”排序作用域和稳定 ID 清单；核对显示器、风扇、蓝牙、CPU 合并行与电池诊断行的归属，并以清单和对应单元测试验证无重复或遗漏。
- [x] 1.2 实现完整顺序的读取、去重、未知 ID 保留、保存和新 ID 默认相对位置合并；用确定性测试验证旧设置升级、版本回退、隐藏项回归及既有用户顺序不变。
- [x] 1.3 将顶层顺序投影到面板展示，纳入显示器区块但保持底部按钮固定；用两渠道可见项夹具验证排序结果不改变采样和统计数据顺序。
- [x] 1.4 将每个指标作用域的保存顺序应用到展开区，保留关闭或暂缺指标的记录；用重新启用、网络状态变化及电池状态变化的测试验证其回到原位置。

## 2. 指标布局与特殊区域

- [x] 2.1 实现按既有静态跨度顺序排布半行和整行指标的共享布局，供正常面板、几何实验路径和设置预览使用；用“半行 A／整行 B／半行 C”、连续整行及奇数半行测试验证占位和尺寸不变。
- [ ] 2.2 将 CPU 温度／热压力合并行及逐核环图／P-E 展示各视为单一排序项；逐核项复用 core-split ID、保持全宽和默认顶部位置，固定进程区仍不可排序；用 Direct 与 App Store 的可见组合测试验证无重复指标或跨区移动。
- [ ] 2.3 接入电池各分页及供电诊断区的独立排序，保持功率流拓扑与其他复杂内容固定；用插电／未插电、分页切换及诊断数据缺失场景验证顺序与落点边界。

## 3. 面板内长按拖动

- [ ] 3.1 建立面板级临时拖动会话、源单元完整外观的静态浮起预览、完整卡片占位和有效插入位置，释放前不持久化；在普通与置顶面板原生运行验证内容和配色、预览跟随指针、相邻项让位且窗口尺寸稳定。
- [ ] 3.2 在整条模块概览行、合格指标格及 CPU 逐核环图区接入长按后拖动，并隔离单击展开、数值复制与滑杆／按钮操作；诊断并修复 App Store 版长按无响应，原生操作各入口，验证短点击与控件手势未回归。
- [ ] 3.3 实现同作用域命中、跨区无效落点、Esc／面板外释放／面板关闭取消及源项消失取消；用确定性落点测试和原生交互验证取消后内存及持久化顺序不变。
- [ ] 3.4 实现滚动边缘自动滚动及置顶面板窗口拖动协调；在触顶限高的长面板中验证可到达末尾位置，并确认非排序区域仍能移动置顶窗口。
- [ ] 3.5 将已提交顺序接入现有几何与展开运动链路，保持中途反转与滚动惯性；运行相关几何／运动测试，并用面板性能夹具及原生目测检查没有持续抖动或材质重建。

## 4. 恢复、辅助操作与文案

- [x] 4.1 实现右键和辅助功能的二维方向移动命令；按当前行列与固定跨度只显示可行的上下左右方向，所有入口共用同一作用域校验；用半行互换、首尾行边界与跨行移动测试验证作用域隔离。
- [x] 4.2 在每个有可排序指标的模块设置页添加仅恢复本模块指标顺序的操作，电池一次覆盖所有分页；验证显隐、展开、其他模块及顶层顺序不变，且没有全局重置或设置内拖动列表。
- [ ] 4.3 为悬停提示、无效落点、移动命令与恢复操作补齐现有本地化；检查现有语言目录中的新增 key，并原生检查浅色／深色外观下提示和焦点可读。

## 5. 集成验收

- [x] 5.1 运行 `MetricWidthAuditTests`、排序模型及面板几何／运动测试；验证拖动前后半行／整行登记、最窄宽度预算与展开行为保持正确。
- [x] 5.2 分别构建 App Store 与 Direct scheme，运行 Direct 测试；验证 App Store 不显示风扇但保留排序 ID、两渠道偏好独立且显示器区块位置可调整。
- [ ] 5.3 重启两版应用并原生检查普通／置顶面板：展开模块、混合宽度指标、滚动边缘、取消、右键／辅助操作、设备／指标暂缺回归及模块内恢复；记录实际通过项和未验证项，不以编译成功代替交互验收。
