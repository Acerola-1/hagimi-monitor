# 原生报表迁移整改与落地任务清单

> 对应审阅计划：`docs/development/native-report-review-2026-09-14.md`
> 基线：`6d89db02`，macOS SDK 26.5，macOS 27.0 运行环境。
> 按照审阅意见，严格按阶段 A～E 推进并完成实机与单元测试客观验收。

## 阶段 A：冻结语义与复现

- [x] A.1 建立并同步 R01～R23 对应任务；重新打开原 change 中待整改项目
- [x] A.2 记录当前 commit、工作区 diff、运行产物、OS/SDK、范围与数据量环境基线
- [x] A.3 梳理旧 HTML 报表→新原生报表的完整字段/图表/导航/导出映射表，明确“保留、强化、合并、遗漏”
- [x] A.4 针对 R01～R08、R14～R17 编写最小失败/边界验证测试夹具（在 `HagimiMonitorTests` 中）
- [x] A.5 建立切模块交互耗时与内存测量基线

## 阶段 B：修正数据和状态（优先于审美）

- [x] B.1 [R01] 明确区分累计量与速率，修复网络与磁盘平均速率（`total / bucketSeconds`）、峰值速率与每日累计量展示
- [x] B.2 [R02] 修复范围切换与刷新竞态，展示状态（Revision、Request ID、请求中与当前范围、起止时间、聚合结果）原子提交
- [x] B.3 [R03] 纠正硬件能力判断，区分“硬件不支持”与“所选时间范围历史无样本”，电池/风扇能力与历史数据脱钩
- [x] B.4 [R04] 修复未知/缺失状态显示成低压力或运行良好的假象；评分 nil 采用中性视觉并提供真实原因说明，区分 0 与 nil
- [x] B.5 [R05] 纠正热状态 0～3 档位乘 100 误当百分比的问题，独立展示档位与温度，温度缺失时仍保留可用热状态
- [x] B.6 [R06] 恢复小时层 7×24 忙碌度热力图（取 CPU、GPU、内存压力的复合最大值），纠正将日数据午夜点当成小时点的问题
- [x] B.7 [R07] 修复折线图缺口（根据采样步长设置断点），CPU 均值与峰值赋予独立 `seriesID` 与 `segmentID`，避免竖线与跨缺口插值
- [x] B.8 [R08] 收紧压力事件“恢复”判定，依据对应维度（内存/热状态）的实际观测，不得以通用 CPU 采样充当恢复证据
- [x] B.9 [R14] 补齐缺失的本地化字符串（消除未提取 key），修复 `{v}` 等模板变量未插值问题，消除技术术语暴露
- [x] B.10 [R15] 恢复每日聚合汇总表（日均、CPU峰值、流量累计、接电占比、覆盖时长等），提供表头列排序能力
- [x] B.11 [R16] 补全遗漏的图表与分析内容（综合多维趋势、显存历史、逐桶峰值、最忙时段洞察、独立电池循环历史等）
- [x] B.12 [R17] 修复日历分组使用 `MM-dd` 导致的跨年与时序错乱，使用稳态 `Date` / `yyyyMMdd` 键与左闭右开开区间
- [x] B.13 [R18] 明确标明进程数据覆盖范围（如近 60 天）与高负载告警的统计作用域

## 阶段 C：解决卡顿与资源释放

- [x] C.1 [R09] 隔离 1 秒实时数据广播与不可变历史报表视图，右栏采用细粒度观察源，避免全页面每秒重组
- [x] C.2 [R09] 后台按视口宽度/时间跨度准备绘图序列，引入保峰值与保断点的降采样机制，建立点数预算
- [x] C.3 [R09] 优化格式化器生命周期（FormatStyle/共享静态），图表悬停引入二分查找
- [x] C.4 [R10] 引入 Window Session ID 与 Task 取消贯穿机制，修复关窗/刷新竞态；最小化/隐藏窗口时停推 1s 实时数据
- [x] C.5 [R11] 将独立 HTML 导出中的数据编码、JSON 序列化与文件写入彻底移至后台 Task，主线程仅保留 NSSavePanel
- [x] C.6 [R12] 重构临时打印会话，支持加载失败清理、取消、完成回调，确保 WebKit 实例可靠销毁；实现已迁移到 `TransientReportPrintSession`，双渠道构建与 Direct 全量测试已通过（helper 进程退出时间不作为唯一判断）

## 阶段 D：内容组织与系统控件

- [x] D.1 [R19] 移除 overview/apps/events/details/insights/thermal 无规格绑定的空白右栏，相应页面使用全宽布局
- [x] D.2 [R20] 重构「本机」全景信息展示：结构化设备信息、芯片、内存、显示、连接、电源与高级详情，隐藏 raw proc 串
- [x] D.3 [R21] 重新定义「概览」首屏任务：回答时间范围、关键资源摘要、异常事件、综合变化时段，避免孤立卡片漂浮
- [x] D.4 [R13] 修复 SF Symbols 映射：使用有效符号（`powerplug`、`display`），系统进程按 WindowServer(display)/通用(terminal) 区分
- [x] D.5 [R22] 调研并落地系统分段导航：基于当前 SDK 26.5 与 macOS 27 环境，规范使用 `.segmented`，明确 27 `.tabs` 演进边界
- [x] D.6 [R23] 优化版面、字体层级与间距，消除固定超窄列与 8~11pt 小字拥挤，接入 `MonitorPalette` 主题令牌

## 阶段 E：收尾验收与报告交付

- [x] E.1 双渠道完整编译：App Store 沙盒（`HagimiMonitor`）与 Direct（`HagimiMonitorDirect`）
- [x] E.2 运行全部统计与报表单元测试，确保测试全绿
- [x] E.3 实机 UI 与多语言验证：中英文切换、深浅外观、窗口 1100pt/默认/全屏缩放
- [x] E.4 严格内存实测对比：记录未开窗、初次开窗、全模块深度切换、关窗后 5/30/60 秒、10 次重复开关物理内存 RSS
- [x] E.5 整理逐项 R01～R23 的落地位置、测试验证证据及对比说明，产出最终整改验收报告

## 阶段 F：用户追加的原生 UI 改进（2026-09-14）

- [x] F.1 以已安装 Xcode 27 构建，保留 macOS 15 最低部署版本，提供项目内构建入口
- [x] F.2 时间范围和本机分类共用原生 tabs Picker，旧系统 segmented 回退
- [x] F.3 重排概览为核心指标、趋势、运行摘要和评分依据，保留数据与导航
- [x] F.4 检查中英文、原生控件语义及真实窗口布局
- [x] F.5 双渠道构建、相关测试、实机切换与拖动验收并记录边界
- [x] F.6 追加原生报表摘要：聚合层数据完整度、紧凑评分依据带与单一 graphical 自定义日期范围选择器；补齐覆盖率边界测试并完成双渠道构建与 Direct 全量测试
