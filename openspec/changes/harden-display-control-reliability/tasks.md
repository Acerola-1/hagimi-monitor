## 1. 接手、基线和测试边界

- [x] 1.1 阅读 AGENTS.md、proposal、design 和四份 delta specs，检查当前工作区及活跃面板变更；建立本 change 的 execution-notes.md，记录基线版本、涉及文件、测试与硬件情况；验收：记录明确本轮不实现输入源/KVM 等扩展，不提交无关代码。
- [x] 1.2 运行 Direct 现有测试作为基线，命令见本文件第 12 组；记录通过/已有失败及日志路径，区分后续新增回归；验收：execution-notes 有实际结果，不将已有失败归因于本变更。
- [x] 1.3 在 DirectOnly 引入可注入的传输、时钟、发现快照、持久化和 Gamma API 边界，保留生产实现；验收：测试可在不访问真实 IOAVService、不改屏幕亮度的情况下调用编排逻辑，Direct 可编译。
- [x] 1.4 为 A2 去重与 A6 门禁重放建立失败回归测试，覆盖外部改值后设回、setMode、begin-only 和面板不可见；验收：能在修复前展示预期失败，测试不依赖 tmp/ddc-audit。

## 2. 身份与连接生命周期（D1；依赖 1）

- [x] 2.1 引入稳定身份、连接 token/generation、匹配证据和歧义结果；验收：相同显示编号不同设备、零序列号同款双屏、相同 EDID 三组夹具不会产生错误唯一身份。
- [x] 2.2 将 Arm64DDCMatcher 拆为只读数据收集与纯匹配决策，先处理强证据，拒绝矛盾和同分任意分配；验收：重排候选枚举顺序不改变正确匹配，歧义保持歧义。
- [x] 2.3 对接连接建立、断开、服务替换事件，清理 max/code/capability/pending/recent/mute 瞬时状态；验收：旧连接回调不能改变新屏，普通读数刷新不增加 generation。
- [x] 2.4 实现显式连接绑定的数据层及失效规则，保留候选与连接摘要供 UI；验收：绑定后仅向选中服务发送，拓扑证据变化时转待绑定，不盲目延续。

## 3. 值模型与目标合并（D2；依赖 2）

- [x] 3.1 增加 observed/desired/historical/applied 与 source/status，使用 requestID 判断回调新旧；验收：50→80→50 的迟到回调、读数晚于目标、无初始读数均有单元测试。
- [x] 3.2 移除永久 lastWrittenValues 省略逻辑，改为同轮交互合并；验收：1.4 的重复设置测试通过，两次独立设置 50 均到达 fake 后端。
- [x] 3.3 实现 150ms 连续节流及交互结束终值提交，媒体键复用目标入口；验收：注入时钟下持续输入不会无限等待停手，100 次输入保持每属性至多一个待写槽且终值正确。
- [x] 3.4 修改 DDCRawConversion 的有限值验证、完整 UInt16 范围和覆盖范围校验；验收：0/100 边界、max 1/100/255/65535、NaN/Infinity、零 max 测试覆盖，去除无依据的 32767 截断。

## 4. 有界调度与故障恢复（D3；依赖 3）

- [x] 4.1 将受控待执行容器与阻塞执行层分离，设置一个全局 in-flight DDC transaction；验收：blocked fake 期间无第二次底层提交，缓冲量只随连接和属性数增长，不随操作次数增长。
- [x] 4.2 请求在入底层与每次重试前检查有效期、generation 和门禁；验收：过期/旧连接/抑制请求不调用 fake I/O，未执行中间目标可被最新目标取代。
- [x] 4.3 实现超时返回未确认且保留真实 in-flight 标志，处理迟到完成；验收：deadline 后 UI 结果返回但不创建替代执行线程，真实返回后旧状态不发布，最新目标可恢复发送。
- [x] 4.4 实现普通故障的 1/2/5/15s 有界退避、手动重新检测和恢复状态；验收：四次自动恢复后停止自动重试，连接事件/手动操作可重新启动；stalled 通道不会因按钮重复点击并发提交。
- [x] 4.5 实现用户写优先、连接间 round-robin 和事务完整串行；验收：一屏持续拖动不会饿死另一屏待写，GetVCP 的请求与响应不被其他事务插入。
- [ ] 4.6 审查新异步边界的隔离和资源生命周期；验收：严格并发构建不新增警告/错误，CF 服务在调用返回前有效，所有 UI 发布在主线程，不使用 unchecked Sendable 掩盖未证明的共享状态。（当前构建仍有新 DirectOnly 并发告警，待单独收敛。）

## 5. 门禁与独立重放（D4；依赖 4）

- [x] 5.1 提取单调时钟状态模型，独立维护系统睡眠、屏幕睡眠、唤醒期限与重配置期限；验收：更短重配置结束不会缩短唤醒抑制，旧 timer 不解除后来的睡眠。
- [x] 5.2 接入屏幕睡眠/唤醒通知并统一 true→false 事件，覆盖 setMode 和 begin-only safety；验收：1.4 门禁测试通过，每轮有效解除只触发一次恢复。
- [x] 5.3 用 scheduler 最新目标取代 controller 独立 suppressedWrites，恢复时先验证连接再重放；验收：面板隐藏仍重放，30→60 只重放 60，设备断开时丢弃，stalled 时保留最新目标等待真实恢复。

## 6. 协议能力与逐属性后端（D5；依赖 4、5）

- [x] 6.1 分离逐 VCP 证据、读取策略、连接健康和后端选择；验收：主码 timeout+备用 unsupported 不导致整体 unsupported，单次丢包保留有效支持证据。
- [x] 6.2 完整验证 DDC 帧结构并保留 raw 字段及结构化错误；验收：checksum/长度/opcode/回显/resultCode/零 max 夹具通过，不支持与坏帧/无应答明确区分。
- [ ] 6.3 默认亮度只用 0x10，将已有 0x13 限定到明确 profile/用户配置，明确其版本与编码；验收：默认路径不会试写 0x13，配置路径仅使用已定义编码，无任意 VCP 扫描。（默认路径已完成，显式配置尚未接入生产桥。）
- [x] 6.4 保留并测试 MCDP29XX 祖先识别及 0xB7/0x37 路由、读写 checksum 夹具；验收：直接祖先和更高祖先均识别，标准连接不误用特殊地址；不凭路由地址改变协议校验规则。
- [x] 6.5 实现每属性 backend，解除亮度软件模式对音量和对比度的禁用；验收：亮度 Gamma、音量 DDC、对比度 DDC 的混合 fake 场景全部到达正确后端。
- [ ] 6.6 实现按设备兼容配置模型：亮度模式、读策略、正常/慢速、max 覆盖、重新检测；验收：设置隔离到设备/属性，手动只写保留原能力证据，错误范围被拒绝。（模型与持久化测试已完成，生产读取/应用尚未接入。）
- [x] 6.7 对虚拟、dummy、无 DDC 服务和 Gamma 不支持设备建立明确分类结果；验收：不因乐观默认而显示虚假可用状态，不扩大到未实现的遮罩/USB 后端。

## 7. 数值刷新与有限确认（D2、D7；依赖 6）

- [x] 7.1 拆分发现、能力探测和数值读取，缓存发现快照；验收：多次正常 polling 不重复枚举注册表或重建服务，拓扑变化触发一次必要发现。
- [x] 7.2 增加 refresh single-flight 和一个待刷新标记，停止不可见周期读；验收：慢刷新期间重复请求不会积压，关闭面板不取消必要恢复与重放。
- [x] 7.3 实现只读已启用属性、拖动期间让位和失败 15s/30s 降频；验收：只写模式自动读计数为零，未开始的周期读不阻挡用户最终写。
- [x] 7.4 实现正常/慢速终态回读、最多两次确认和量化容差；验收：用虚拟时钟测试确认成功、偏差 1 raw、持续不一致、连接更换以及新交互取消旧确认。

## 8. 软件调光和持久化（D6、D10；依赖 6、7）

- [x] 8.1 Gamma API 返回结构化结果，成功后才更新 applied 状态和成功值；验收：fake Gamma 失败时不更新成功历史，不向 UI 返回 written。
- [x] 8.2 获取与恢复基线传输表，绑定连接代次，避免自身表再次作为基线；验收：50→50 不累乘，100 恢复基线，取基线失败不假称可无损恢复。
- [x] 8.3 将重施加限制为用户操作和适用系统事件，处理色彩基线失效；验收：普通轮询不调用 Gamma，唤醒恢复只应用本会话正确基线和因子，错误恢复可观测。
- [x] 8.4 增加 v2 持久化命名空间与唯一身份迁移；验收：旧历史值标记未确认，零序列号冲突不迁移，硬件历史不自动变成软件因子，旧 key 保留以支持回滚。
- [x] 8.5 实现重启后的显式软件模式恢复、切回硬件和正常退出恢复；验收：成功/失败恢复路径均有 fake 测试，失败不宣称切换完成，软件和硬件值各自保存。

## 9. 媒体键与音量恢复（D8；依赖 2、3、6、8）

- [x] 9.1 提供默认音频设备 UID/类型/可控性及可用映射信息，纯逻辑选择目标；验收：双屏鼠标 B、音频 A 时选 A，未知映射不猜测，系统可控输出交还系统。
- [ ] 9.2 实现 audio UID 到 DisplayIdentity 的显式绑定与断开失效；验收：重接同身份恢复合法绑定，身份变更不向错误屏发送。（选择器模型已完成，用户绑定入口尚未接入。）
- [x] 9.3 改造调节方法返回是否接受操作，亮度按可控集合执行既有鼠标/单屏策略；验收：不支持、未知值基准、键盘锁或权限缺失不伪造成功，不错误吞普通键。
- [x] 9.4 静音前捕获可靠非零值，统一最近值→持久化→兜底的恢复；验收：历史70、实读25时解除恢复25，并覆盖重启70、无历史、按住静音 repeat、细步进。

## 10. 用户界面和诊断（D9；依赖 6、8、9）

- [x] 10.1 以现有面板和设置结构制作兼容页 HTML 原型，包含未知值、软件模式、待绑定、只写和通信异常状态；验收：原型可本地打开，布局覆盖中文/英文；仅本轮必要配置流，不重构面板动画。
- [x] 10.2 为控制区接入 optional 值及来源状态、软件模式标识和恢复入口；验收：未知显示 --、只写显示上次设置、Gamma失败显示异常，单次丢包不重复弹窗。
- [ ] 10.3 接入逐屏兼容设置、连接绑定和音频绑定，拒绝不合法输入；验收：选择软件亮度后其他 DDC 参数仍可用，歧义屏选择前不发写入，设置可重启保留。（数据模型已完成，用户设置/绑定页尚未接入。）
- [ ] 10.4 实现每连接100条环形诊断、结构化导出和复制操作；验收：1000事件只保留最新100条，包含 requestID/代次/耗时/错误/范围/来源，默认脱敏且无自动上传。（缓冲区与导出器已完成，生产收集和复制按钮尚未接入。）
- [x] 10.5 为新增文案补齐 xcstrings 两语，补 Slider accessibilityLabel/Value/Hint；验收：本地化审计通过，VoiceOver 可辨识屏幕/属性/来源/状态，用户界面无渠道内部术语。

## 11. 集成回归和清理（依赖 1–10）

- [x] 11.1 用脚本化 fake 后端串起发现→调节→外部改值→重配置→重放→断开→编号复用完整流程；验收：最终值正确、无旧连接写入、资源有界，执行不触碰真实显示器。
- [x] 11.2 将四份 specs 的每个 Scenario 映射到具体测试名称或硬件验收记录，写入 execution-notes；验收：无无主场景，不用字典 setter 测试代替编排正确性测试。
- [x] 11.3 删除旧去重/重复待写/重复探测链路，清理与最终实现矛盾的注释；验收：审查不存在“两套状态互相回写”，新增注释仅说明当前设计和原因。
- [x] 11.4 检查 App Store 条件编译、DirectOnly 文件归属、依赖和设置接线；验收：App Store 不链接新私有符号、不展示控制配置，无额外第三方运行依赖。

## 12. 构建、真机验收和交接（依赖 11）

- [x] 12.1 执行 Direct 完整测试：`xcodebuild -project hagimi-monitor.xcodeproj -scheme HagimiMonitorDirect -configuration Debug -destination 'platform=macOS' -derivedDataPath tmp/dd-direct test`；验收：日志和结果路径写入 execution-notes，新回归全部通过，已有失败单独说明。
- [x] 12.2 构建 App Store：`xcodebuild -project hagimi-monitor.xcodeproj -scheme HagimiMonitor -configuration Debug -destination 'platform=macOS' -derivedDataPath tmp/dd-appstore build`；验收：构建成功，产物与 Direct 隔离。
- [x] 12.3 构建 Direct：`xcodebuild -project hagimi-monitor.xcodeproj -scheme HagimiMonitorDirect -configuration Debug -destination 'platform=macOS' -derivedDataPath tmp/dd-direct build`；验收：构建成功，记录新警告检查结果。
- [ ] 12.4 重启两个本次构建版本供用户观察，核验兼容页、状态文案、滑块、展开动画和 VoiceOver；验收：记录实际启动路径及目测结果，不终止无关应用进程。
- [ ] 12.5 在当前可用设备执行 design 最低硬件矩阵并附脱敏诊断；验收：记录型号、连接、HDR、步骤及实际画面/OSD结果，包含外部改值后设回、后台媒体键、唤醒、重配置和软件恢复；没有硬件则保持本项未完成并说明。
- [ ] 12.6 核对扩展矩阵 USB-C/DP、HDMI、MCDP29XX、扩展坞、双同型号、只写、Gamma不支持、HDR；验收：逐项列 pass/fail/not-run 及原因，禁止将不可获得硬件标成通过；此项完成表示覆盖情况已如实记录，不表示全部兼容。
- [x] 12.7 再运行 `openspec validate harden-display-control-reliability --strict` 并审查差异；交付功能变化、测试证据、未验证设备、已知限制及回滚说明；验收：规格校验通过，所有勾选任务有证据，不自动提交/合并/归档。
