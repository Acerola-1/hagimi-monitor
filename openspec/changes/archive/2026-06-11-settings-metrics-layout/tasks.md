## 1. 文案修改

- [x] 1.1 将 `ModuleSettingsView` 中 "检测项目" 改为 "监测项目"
- [x] 1.2 确认 caption 文案不变（"最多选择 4 项用于主面板展示。"）

## 2. 布局重构

- [x] 2.1 将 `ModuleSettingsView` 中 `ForEach` 垂直列表替换为 `LazyVGrid` 双列网格
- [x] 2.2 配置 `GridItem(.flexible())` × 2，设置合适的 spacing
- [x] 2.3 验证 `MetricSelectionRow` 在双列布局下正常显示

## 3. 交互验证

- [ ] 3.1 验证勾选/取消勾选功能正常
- [ ] 3.2 验证最多4项限制仍然生效
- [ ] 3.3 验证禁用态（达到4项后未选中的项变灰）正常
- [ ] 3.4 验证重置默认值按钮恢复全选

## 4. 构建与运行

- [x] 4.1 构建项目，确认无编译错误
- [ ] 4.2 运行应用，验证各模块设置页面的「监测项目」显示为双列布局
