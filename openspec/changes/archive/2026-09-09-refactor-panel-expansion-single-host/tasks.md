## 1. 实验入口与可复现基线

- [x] 1.1 检查工作区改动归属并准备 `codex/panel-expansion-single-host` 实验分支，保留原生产面板入口；验证分支、基线提交与变更范围已记录，未改动其他用户工作，也未执行旧 compositor 计划。
- [x] 1.2 记录两个渠道、菜单栏/钉住面板的收起和展开画面、窗口尺寸与事件行为；核对 340/328pt 宽度、8pt 顶部、6pt 侧边/底部/行距、14/20pt 圆角及底部按钮随主体滚动，保存基线到 `tmp/panel-single-host/`。
- [x] 1.3 按 design 的预热和重复次数采集 Release、60Hz 正常与可复现 CPU 负载基线，记录数据夹具、负载参数、Instruments 栈、帧间隔和可见/隐藏内存；验证能够复现并区分展开递归布局阻塞与系统调度延迟。

## 2. 版本化尺寸与统一几何

- [x] 2.1 实现带稳定分区 ID、层级和 revision 的自然尺寸登记，按完整行头/明细与真实卡片宽度测量；验证同版本普通读数刷新不重测，宽度/语言/字体/结构变化会失效，迟到旧版本结果被丢弃。
- [x] 2.2 实现纯几何求解与 `PanelFrame`，集中计算卡片位置、主体文档高、视口高和窗口内容高；用多行组合、不同宽度和封顶/解封测试验证尺寸有效、间距恒定、6pt 底边距及无累计舍入误差。
- [x] 2.3 为嵌套分区建立只计一次的父子高度和可见区域计算；测试父子同时开关、外层关闭内层展开、设备删除及模块重排，确认窗口高度、ID 对应和隐藏区域均正确。
- [x] 2.4 实现初始尺寸未齐的准备状态与运动期间几何换版重基准；测试首次快速点击、旧测量晚到、语言切换和真实内容结构改变，确认无零高度启动、错误回填或仍有效轨迹的速度重置。

## 3. 单宿主三卡片样机与布局边界

- [x] 3.1 建立使用生产 Header、CPU 明细、两张相邻卡片和底部按钮的原生实验入口；验证面板只有一个 NSHostingView，保留完整 CompatibleGlassContainer、窗口底材质、原有行级 modifier 与真实 NSPanel。
- [x] 3.2 实现读取已知几何的面板根布局和 AccordionLayout，替代候选路径中的自然高度反馈/根 fixedSize 查询；通过提议与测量计数确认改变窗口高度不向昂贵叶子传播变化的提议，不在每帧主动测量兄弟内容。
- [x] 3.3 实现 CardShell 与 DetailViewportLayout，复用原行头和明细内部 padding，材质按当前外壳高度绘制；对照端点和中间帧验证行头完整、零揭示无明细像素、卡片底部 continuous 14pt 圆角及外层留白不变。
- [x] 3.4 将帧观察限定于轻量表现包装层，接入局部命中形状、语义交互门控和焦点/辅助功能隐藏；验证指标内容更新不被仅按 ID 判等冻结，部分揭示按钮/滑杆只在可见区域命中，收起时隐藏控件不可访问。

## 4. 单时钟运动与真实窗口提交

- [x] 4.1 为每个面板建立私有运动协调状态，使用 0.32/0.82 弹簧按绝对时间采样；用位置/速度连续性、有限差分速度、随机中断时刻和不同采样间隔测试验证反转接续及帧率无关性。
- [x] 4.2 集中处理非负揭示、自然高度过冲和屏幕视口上限，按位置与速度共同判定收敛；测试零边界附近快速反转、超高展开和尾段微振，确认不裁行头、不露明细残帧、不提前归零速度或末帧补调窗口。
- [x] 4.3 实现带 frameID 的单一提交适配，在无隐式动画事务中应用表现几何与真实窗口 frame；验证菜单栏/钉住顶边锚定、content/frame 换算、原生阴影和事件区域，并记录 `display: false/true` 与布局触发顺序的原生对照结果。
- [x] 4.4 接入只在活动期存在的屏幕 display link，保留并延长展开刷新门控至实际收敛；测试反复点击、隐藏、减少动态效果和换屏，确认时钟不倒退、收敛/隐藏后停止、迟到回调不恢复窗口或采样竞争。

## 5. 样机封顶滚动与迁移门槛

- [x] 5.1 在样机中接入受限高度的原有主体 ScrollView，让自动揭示读取同一帧偏移，按视觉顺序选择最后一个新展开目标；测试跨上限、底部收起、无溢出和多行切换，确认 Header 固定、按钮随主体滚动且渐隐不闪现。
- [x] 5.2 实现用户滚轮/触控板对自动偏移的连续接管；原生验证反向滚动与展开并发、触控板惯性及视口解封，确认偏移只有一个有效控制方且无空洞或独立追赶动画。
- [ ] 5.3 按 design 第 7 节完成样机性能、视觉、实际帧同步、事件和内存五项门槛，输出含原始数据路径的验收记录；必须证明递归布局耗时降低至少 50%、相关 40–50ms 阻塞消失且窗口/内容无可重复错帧，全部通过后才执行第 6 组，失败时保留生产入口并据证据修订方案。

## 6. 逐类生产迁移

- [x] 6.1 将通用指标行迁入已通过门槛的包装层，保持核心环、指标格、进程/风扇/存储内容与数据契约；逐类构建并重启两个渠道，与固定数据基线对照收起、展开、反转及最坏值排版。
- [x] 6.2 迁移网络、电池等专用行的展开入口，避免遗留局部 spring 与统一几何竞争；构建并重启两个渠道，验证专用明细、控件命中、采样刷新和不可用内容降级无变化。
- [x] 6.3 迁移显示器信息、控制与内嵌区域，使用已验证的父子几何和批量目标；测试无/单/多显示器、设备移除和父子同时切换，并验证 App Store/Direct 编译边界、隐藏重置及默认展开行为。
- [x] 6.4 将两个生产面板控制器接入同一实现且分别持有状态，接入既有隐藏资源回收；测试同时显示、拖动钉住面板、关闭重开、主题/语言切换，确认运动互不牵动、位置保存及首帧尺寸正确。

## 7. 全量验收与旧链路清理

- [ ] 7.1 在完整面板重跑样机门槛和真实采样回归，覆盖 macOS 15/26+、60Hz/高刷新率、低电量模式与已记录 CPU 负载；输出前后 p50/p95/p99、慢帧归因、独立视觉捕获与可见/隐藏内存结果，缺少环境的项目保持明确未验收状态。
- [ ] 7.2 完整验收通过后移除旧展开高度预测、独立 PanelWindowSpring、常规末帧校准和遗留展开隐式几何动画；检查调用点并重跑单行/全量反转与显示器嵌套回归，确认候选路径不再依赖旧动画链且窗口只有一个尺寸写入出口。
- [x] 7.3 使用 `xcodebuild -project hagimi-monitor.xcodeproj -scheme HagimiMonitorDirect -configuration Debug -destination 'platform=macOS' test` 运行测试；确认新增几何/运动测试、MetricWidthAuditTests 及现有测试通过，保存结果。
- [x] 7.4 使用 `tmp/dd-appstore` 与 `tmp/dd-direct` 隔离 DerivedData 分别构建 Debug 的 HagimiMonitor / HagimiMonitorDirect，重启两个版本供目测；验证材质、圆角、点外关闭、部分揭示命中、键盘/VoiceOver 和两个渠道启动行为。
- [ ] 7.5 在最终验收通过后移除实验切换入口，更新 AGENTS.md 与相关注释中错误的“布局必定只执行一次”等描述；复查无硬编码新色值、重复 padding、过程性注释或用户可见渠道术语，运行 OpenSpec 严格校验并交付证据索引，未经用户要求不提交或归档。

## 本轮接入状态（2026-09-06）

全模块单宿主管线接入与 4 项核心收尾（边界零交叉平滑反转、子组 28pt 动态缩进解算、惯性减速与自动滚动协调、双渠道显示器模块接入）已完成并通过单元测试与隔离构建验证。实验 Feature Flag 开关与旧链路保持保留，待后续用户实际目测确认满意后再执行旧链路清理与 OpenSpec 最终归档。
